import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF2BBB6E); // vert principal
  static const primaryLight = Color(0xFFE8F8EF); // vert clair (fond)
  static const secondary = Color(0xFF3B82F6); // bleu
  static const secondaryLight = Color(0xFFEBF2FF); // bleu clair
  static const accent = Color(0xFF8B5CF6); // violet (boutique)
  static const accentLight = Color(0xFFF3F0FF); // violet clair
  static const background = Color(0xFFF8FAF8); // fond de l'app
  static const card = Color(0xFFFFFFFF); // fond des cartes
  static const text = Color(0xFF1A1A1A); // texte principal
  static const textSecondary = Color(0xFF6B7280); // texte gris
  static const border = Color(0xFFE5E7EB); // bordures
  static const success = Color(0xFF10B981); // vert succès
  static const warning = Color(0xFFF59E0B); // orange alerte
  static const danger = Color(0xFFEF4444); // rouge erreur
}
