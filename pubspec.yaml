name: devmobile
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^4.17.0
  cupertino_icons: ^1.0.8
  firebase_core: ^2.32.0
  fl_chart: ^0.68.0
  flutter_map: ^8.2.2
  flutter_map_location_marker: ^10.2.0
  flutter_typeahead: ^6.0.0
  geolocator: ^14.0.2
  http: ^1.6.0
  latlong2: ^0.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/data/security_zones.json
    - lib/data/rewards.json
